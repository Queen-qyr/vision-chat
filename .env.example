# OpenAI API 配置（必填）
OPENAI_API_KEY=sk-your-api-key-here

# 模型选择（成本控制：gpt-4o-mini 更便宜，gpt-4o 视觉理解更强）
OPENAI_MODEL=gpt-4o-mini

# TTS 语音（可选：alloy, echo, fable, onyx, nova, shimmer）
OPENAI_TTS_VOICE=nova

# 服务端口
PORT=3001
